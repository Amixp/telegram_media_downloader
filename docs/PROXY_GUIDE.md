# 🔐 Руководство по настройке прокси

Telegram Media Downloader поддерживает работу через прокси-серверы для обхода блокировок или обеспечения конфиденциальности.

## 🎯 Поддерживаемые типы прокси

- **SOCKS4** - базовый SOCKS прокси
- **SOCKS5** - продвинутый SOCKS прокси с аутентификацией
- **HTTP** - стандартный HTTP прокси

## 🚀 Быстрая настройка

### 1. Базовая конфигурация

Добавьте секцию `proxy` в `config.yaml`:

```yaml
proxy:
  scheme: socks5
  hostname: 127.0.0.1
  port: 1080
```

### 2. С аутентификацией

```yaml
proxy:
  scheme: socks5
  hostname: proxy.example.com
  port: 1080
  username: myuser
  password: mypass
```

## 📋 Примеры конфигураций

### SOCKS5 (рекомендуется)

```yaml
proxy:
  scheme: socks5
  hostname: 127.0.0.1
  port: 1080
```

**Преимущества:**
- ✅ Поддержка UDP (для голосовых звонков)
- ✅ Аутентификация
- ✅ Лучшая производительность

### SOCKS4

```yaml
proxy:
  scheme: socks4
  hostname: 192.168.1.100
  port: 1080
```

**Особенности:**
- ✅ Простой и быстрый
- ❌ Нет аутентификации
- ❌ Только TCP

### HTTP прокси

```yaml
proxy:
  scheme: http
  hostname: proxy.company.com
  port: 8080
  username: employee
  password: secret123
```

**Использование:**
- ✅ Корпоративные сети
- ✅ Аутентификация
- ⚠️ Медленнее SOCKS5

## 🔧 Локальные прокси-серверы

### Shadowsocks

```yaml
proxy:
  scheme: socks5
  hostname: 127.0.0.1
  port: 1080  # Порт по умолчанию для Shadowsocks
```

### Tor

```yaml
proxy:
  scheme: socks5
  hostname: 127.0.0.1
  port: 9050  # Порт по умолчанию для Tor
```

### V2Ray / Xray

```yaml
proxy:
  scheme: socks5
  hostname: 127.0.0.1
  port: 10808  # Порт SOCKS в V2Ray
```

## ✅ Валидация конфигурации

При запуске скрипт автоматически проверяет конфигурацию прокси:

### Успешная настройка

```
🔐 Использую прокси: socks5://127.0.0.1:1080
   Аутентификация: myuser
```

### Ошибки конфигурации

```
❌ Некорректный тип прокси: socks6. Допустимые: socks4, socks5, http
❌ Некорректный порт прокси: 99999 (должен быть 1-65535)
❌ hostname прокси должен быть непустой строкой
```

## 🧪 Проверка подключения

### 1. Запустите скрипт

```bash
python media_downloader.py
```

### 2. Проверьте логи

Успешное подключение:
```
🔐 Использую прокси: socks5://127.0.0.1:1080
✅ Подключено к Telegram
```

Ошибка подключения:
```
❌ Не удалось подключиться через прокси
```

## 🐛 Решение проблем

### Прокси не работает

1. **Проверьте, запущен ли прокси-сервер:**
   ```bash
   # Для SOCKS5
   netstat -an | grep 1080
   
   # Для HTTP
   netstat -an | grep 8080
   ```

2. **Проверьте доступность:**
   ```bash
   # Для SOCKS5
   curl --socks5 127.0.0.1:1080 https://telegram.org
   
   # Для HTTP
   curl --proxy http://127.0.0.1:8080 https://telegram.org
   ```

3. **Проверьте логи прокси-сервера**

### Медленная загрузка

1. Попробуйте другой тип прокси (SOCKS5 обычно быстрее HTTP)
2. Проверьте загрузку канала прокси-сервера
3. Уменьшите `max_parallel_downloads` в конфиге

### Ошибки аутентификации

1. Проверьте правильность username и password
2. Убедитесь, что прокси-сервер поддерживает аутентификацию
3. Проверьте спецсимволы в пароле (могут требовать экранирования)

## 🔒 Безопасность

### Рекомендации

1. **Не используйте публичные прокси** - они могут перехватывать трафик
2. **Используйте собственный прокси-сервер** (Shadowsocks, V2Ray, WireGuard)
3. **Не храните пароли в открытом виде** - используйте переменные окружения:

```yaml
proxy:
  scheme: socks5
  hostname: 127.0.0.1
  port: 1080
  username: ${PROXY_USER}
  password: ${PROXY_PASS}
```

### Проверенные решения

- ✅ **Shadowsocks** - простой и надежный
- ✅ **V2Ray/Xray** - продвинутый с множеством протоколов
- ✅ **Tor** - максимальная анонимность (медленно)
- ✅ **WireGuard** - VPN вместо прокси (рекомендуется)

## 📊 Производительность

### Сравнение типов прокси

| Тип | Скорость | Безопасность | Сложность настройки |
|-----|----------|--------------|---------------------|
| SOCKS5 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| SOCKS4 | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| HTTP | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |

### Оптимизация

```yaml
proxy:
  scheme: socks5
  hostname: 127.0.0.1
  port: 1080

download_settings:
  max_parallel_downloads: 3  # Меньше при использовании прокси
  pagination_limit: 50       # Меньшие пакеты для стабильности
```

## 🌍 Публичные прокси (не рекомендуется)

⚠️ **Внимание:** Публичные прокси могут быть небезопасны!

Если всё же используете публичные прокси:
1. Меняйте их регулярно
2. Не передавайте конфиденциальные данные
3. Проверяйте на вирусы скачанные файлы

## 📚 Дополнительные ресурсы

- [Telegram Proxy Documentation](https://core.telegram.org/api/proxy)
- [Shadowsocks Setup Guide](https://shadowsocks.org/en/index.html)
- [V2Ray Documentation](https://www.v2ray.com/)
- [Tor Project](https://www.torproject.org/)

---

**Обновлено:** 2026-01-04
